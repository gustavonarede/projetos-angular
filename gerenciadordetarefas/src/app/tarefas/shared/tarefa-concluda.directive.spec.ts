import { TarefaConcludaDirective } from './tarefa-concluda.directive';

describe('TarefaConcludaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarefaConcludaDirective();
    expect(directive).toBeTruthy();
  });
});
